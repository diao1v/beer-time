#pragma once
#include <stdint.h>

// Placeholder constants. The current firmware generates the celebration
// frames at runtime (rainbow sweep) so we don't ship pixel literals
// before real art is authored. When you generate frames with
// tools/png_to_rgb565.py, this file will be overwritten.

#define CELEBRATE_FRAMES 8
#define CELEBRATE_W      64
#define CELEBRATE_H      64
